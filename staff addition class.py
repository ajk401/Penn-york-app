import csv
class Staff(object):

    def __init__(self, id):
        super(Staff, self).__init__()

        self.id = id
        print("test")
        def Create_staff(self, id):

            with open("stafflist.csv", "a+") as stafflist:

                print(type + str(age) + first_name + last_name + break_group)
                stafflist.writelines(type + ",")


Test = Staff(1)


def get_staff(filters, filter_types):
    filter_types.split(",")
